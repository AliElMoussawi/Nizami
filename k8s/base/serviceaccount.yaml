apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets-sa
  labels:
    app: backend
  # annotations:
    # If using IRSA (IAM Roles for Service Accounts), uncomment and set:
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/external-secrets-role
automountServiceAccountToken: true

